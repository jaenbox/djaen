{# app/Resources/views/Administrador/show.html.twig #}
{% extends "base.html.twig" %}
{% trans_default_domain 'AppBundle' %}

{% block title %} Administrador {% endblock %}
{% block stylesheets %}
	{{ parent() }}
{% endblock %}

{% block body %}
	<div class="container contenedor">
		<div class="row">
       		<div class="col-xs-12">
       			<div class="col-xs-12 col-sm-12">
    				<p style="padding:10px;"></p>
    			</div>
				<ol class="breadcrumb">
		        	<li><a href="{{ path('index') }}">{{ 'bread.home' | trans }}</a></li>
		        	<li><a href="{{ path('user') }}">{{ 'bread.client' | trans }}</a></li>
		            <li>{{ 'bread.show' | trans }}</li>
		       	</ol>
		    </div>
		</div>
		<h2>{{ 'table.client' | trans }} {{ cliente.username }}</h2>
		<hr>
		<a href="{{ path('user') }}"><button type="button" class="btn btn-default">{{ 'table.button' | trans }}</button></a>
		<hr>
		<table class="table">
			<tbody>
					<tr><th><i class="fa fa-user fa-2x"/></th><td>{{ cliente.id }}</td></tr>
					<tr><th>{{ 'table.username' | trans }}</th><td>{{ cliente.username }}</td></tr>
					<tr><th>{{ 'table.surname' | trans }}</th><td>{{ cliente.surname }}</td></tr>
					<tr><th>DNI</th><td>{{ cliente.dni }}</td></tr>
					<tr><th><i class="fa fa-at fa-2x"/></th><td>{{ cliente.email }}</td></tr>
					<tr><th><i class="fa fa-phone-square fa-2x"/></th><td>{{ cliente.phone }}</td></tr>
					<tr><th>{{ 'table.address' | trans }}</th><td>{{ cliente.address }}</td></tr>
			</tbody>
		</table>
		<hr>
	</div>
{% endblock %}
{% block javascripts %}{% endblock %}
